<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.halolampung">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ListParActivity"
            android:label="Pariwisata"
            android:parentActivityName=".PariwisataActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PariwisataActivity"
            android:label="Pariwisata"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".CekHarga"
            android:label="Cek Harga Pariwisata"
            android:parentActivityName=".PariwisataActivity" />
        <activity
            android:name=".DeveloperActivity"
            android:label="Developer"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>